// @mui
import { createTheme } from '@mui/material/styles';

const lightTheme = createTheme({
  palette: {
    mode: 'light',
    background: {
      default: 'tomato',
    },
  },
});

export default lightTheme;
